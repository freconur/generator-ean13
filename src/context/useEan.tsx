import { useGlobalContextDispatch } from "./useGlobalContext"

export const useEan = () => {

    const dispatch = useGlobalContextDispatch()
    const createEan = () => {
        
    }

    
    return {
        createEan
    }
}